{
  "name": "webscraper",
  "version": "1.2.0",
  "description": "A console app that parses through a certain eShop store front containing asynchronously loading scripts and scrapes the information into a JSON object.",
  "main": "index.js",
  "scripts": {
    "start": "node index",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "John Higgins",
  "license": "MIT",
  "dependencies": {
    "cheerio": "^1.0.0-rc.2",
    "moment": "^2.22.2",
    "puppeteer": "^1.5.0"
  }
}
